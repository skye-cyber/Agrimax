<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AgriMax - Farm Information</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    /* Additional styles for animations */
    input:focus {
      animation: pulse 0.5s ease-out;
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(99, 102, 241, 0.4);
      }
      70% {
        box-shadow: 0 0 0 8px rgba(99, 102, 241, 0);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(99, 102, 241, 0);
      }
    }
  </style>
</head>
<body class="h-screen flex items-center justify-center bg-gradient-to-br from-green-400 via-blue-500 to-purple-600">

    <!-- Farm Information Form -->
    <div class="bg-gradient-to-r from-white to-gray-100 bg-opacity-90 backdrop-blur-lg rounded-xl shadow-2xl p-8 w-full max-h-l md:max-h-full max-w-md z-10">
        <h2 class="text-3xl font-bold mb-6 text-center text-gray-800">Farm Information</h2>
        <form method="POST" action="{% url 'farminformation' %}" class="space-y-6">
            {% csrf_token %}
            <!-- Farm Name -->
            <div>
                <label for="farm_name" class="block text-sm font-medium text-gray-700">Farm Name</label>
                <input type="text" required id="farm_name" name="farm_name" value="{{ form.farm_name.value|default:'' }}"
                       class="appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-lg placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm transition duration-300 ease-in-out transform hover:-translate-y-1">
                {% for error in form.errors.username %}
                    <p class="text-sm text-red-500">{{ error }}</p>
                {% endfor %}
            </div>
            <!-- Farm Location -->
            <div>
                <label for="farm_location" class="block text-sm font-medium text-gray-700">Farm Location</label>
                <input type="text" required id="farm_location" name="farm_location" value="{{ form.farm_location.value|default:'' }}"
                       class="appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-lg placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm transition duration-300 ease-in-out transform hover:-translate-y-1">
                {% for error in form.errors.username %}
                    <p class="text-sm text-red-500">{{ error }}</p>
                {% endfor %}
            </div>
            <!-- Submit Button -->
            <div>
                <button type="submit"
                        class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-lg text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-4 focus:ring-indigo-500 transition-transform transform hover:scale-105">
                    Submit
                </button>
            </div>
        </form>
    </div>
</body>
</html>
